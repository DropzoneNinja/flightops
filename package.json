{
  "name": "flightops",
  "version": "4.1.0",
  "private": true,
  "scripts": {
    "version": "npm --no-git-tag-version version $npm_package_version --prefix frontend && npm --no-git-tag-version version $npm_package_version --prefix backend && git add frontend/package.json backend/package.json"
  }
}
