/**
 * Scoring engine version identifier.
 * Increment this when any scoring logic changes so historical scores
 * can be identified and re-run via the reanalyze endpoint.
 */
export const SCORING_VERSION = '1.0';
